// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ProtonMail/gluon/imap"
	"github.com/ProtonMail/gluon/internal/db/ent/uidvalidity"
)

// UIDValidityCreate is the builder for creating a UIDValidity entity.
type UIDValidityCreate struct {
	config
	mutation *UIDValidityMutation
	hooks    []Hook
}

// SetUIDValidity sets the "UIDValidity" field.
func (uvc *UIDValidityCreate) SetUIDValidity(i imap.UID) *UIDValidityCreate {
	uvc.mutation.SetUIDValidity(i)
	return uvc
}

// Mutation returns the UIDValidityMutation object of the builder.
func (uvc *UIDValidityCreate) Mutation() *UIDValidityMutation {
	return uvc.mutation
}

// Save creates the UIDValidity in the database.
func (uvc *UIDValidityCreate) Save(ctx context.Context) (*UIDValidity, error) {
	var (
		err  error
		node *UIDValidity
	)
	if len(uvc.hooks) == 0 {
		if err = uvc.check(); err != nil {
			return nil, err
		}
		node, err = uvc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*UIDValidityMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = uvc.check(); err != nil {
				return nil, err
			}
			uvc.mutation = mutation
			if node, err = uvc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(uvc.hooks) - 1; i >= 0; i-- {
			if uvc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = uvc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, uvc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*UIDValidity)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from UIDValidityMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (uvc *UIDValidityCreate) SaveX(ctx context.Context) *UIDValidity {
	v, err := uvc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (uvc *UIDValidityCreate) Exec(ctx context.Context) error {
	_, err := uvc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (uvc *UIDValidityCreate) ExecX(ctx context.Context) {
	if err := uvc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (uvc *UIDValidityCreate) check() error {
	if _, ok := uvc.mutation.UIDValidity(); !ok {
		return &ValidationError{Name: "UIDValidity", err: errors.New(`ent: missing required field "UIDValidity.UIDValidity"`)}
	}
	return nil
}

func (uvc *UIDValidityCreate) sqlSave(ctx context.Context) (*UIDValidity, error) {
	_node, _spec := uvc.createSpec()
	if err := sqlgraph.CreateNode(ctx, uvc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (uvc *UIDValidityCreate) createSpec() (*UIDValidity, *sqlgraph.CreateSpec) {
	var (
		_node = &UIDValidity{config: uvc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: uidvalidity.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: uidvalidity.FieldID,
			},
		}
	)
	if value, ok := uvc.mutation.UIDValidity(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeUint32,
			Value:  value,
			Column: uidvalidity.FieldUIDValidity,
		})
		_node.UIDValidity = value
	}
	return _node, _spec
}

// UIDValidityCreateBulk is the builder for creating many UIDValidity entities in bulk.
type UIDValidityCreateBulk struct {
	config
	builders []*UIDValidityCreate
}

// Save creates the UIDValidity entities in the database.
func (uvcb *UIDValidityCreateBulk) Save(ctx context.Context) ([]*UIDValidity, error) {
	specs := make([]*sqlgraph.CreateSpec, len(uvcb.builders))
	nodes := make([]*UIDValidity, len(uvcb.builders))
	mutators := make([]Mutator, len(uvcb.builders))
	for i := range uvcb.builders {
		func(i int, root context.Context) {
			builder := uvcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*UIDValidityMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, uvcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, uvcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, uvcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (uvcb *UIDValidityCreateBulk) SaveX(ctx context.Context) []*UIDValidity {
	v, err := uvcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (uvcb *UIDValidityCreateBulk) Exec(ctx context.Context) error {
	_, err := uvcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (uvcb *UIDValidityCreateBulk) ExecX(ctx context.Context) {
	if err := uvcb.Exec(ctx); err != nil {
		panic(err)
	}
}
